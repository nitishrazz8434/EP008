# Runtime dependencies for the HealthPulse AI backend.
# Keep this file focused: it should contain only packages needed to run the API/model.
fastapi>=0.115,<1
uvicorn[standard]>=0.30,<1
httpx>=0.28,<1
pydantic>=2.7,<3
python-multipart>=0.0.9,<1
scikit-learn>=1.6,<2
